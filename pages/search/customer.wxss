/* pages/search/customer.wxss */
.search-scroll-view{
  width: 100vw;
  height: calc(100% - 100rpx);
}
.customer-item:active{
  background: var(--light-gray-color);
}
.customer-item > view{
  justify-content: space-between;
}
.text-bold{
  font-weight: 900;
}